[project]
name = "binsim-analyzer"
version = "0.1.0"
description = "Automated binary similarity analysis for open-source libraries"
readme = "README.md"
dependencies = [
    "pyyaml",
    "pyelftools",
    "pylzma",
    "msgpack",
    "pyghidra",
    "ghidra-stubs==12.0.4"
]
